# Domain name Caddy should serve (used in Caddyfile via {$DOMAIN})
# For local testing you can leave it unset and Caddy will default to localhost.
DOMAIN=example.com

# Base URL prepended to item links in generated RSS feeds.
# Set this to your site's root URL (no trailing slash).
# If unset, RSS item links will be relative paths.
BASE_URL=https://example.com

# Editor dashboard credentials (required to enable /edit).
# If either is unset, the editor is disabled and /edit returns 404.
EDITOR_USERNAME=admin
EDITOR_PASSWORD=changeme
